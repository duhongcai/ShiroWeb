<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.RoleDao">
    <resultMap id="roleResult" type="entry.Role">
        <id column="id" property="id"/>
        <result column="role" property="roleName"/>
        <result column="description" property="description"/>
        <result column="avaliable" property="avaliable"/>
    </resultMap>

    <select id="getAllRoles" resultMap="roleResult">
       SELECT ID,ROLE,DESCRIPTION,AVALIABLE FROM SYS_ROLES
    </select>
</mapper>